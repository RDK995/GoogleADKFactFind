# Provider switch: vertex | openai
AGENT_PROVIDER=vertex

# Vertex config (used when AGENT_PROVIDER=vertex)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
# Optional: GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# OpenAI config (used when AGENT_PROVIDER=openai)
# OPENAI_API_KEY=your-openai-api-key

# Optional model override.
# Defaults:
# - vertex -> gemini-2.0-flash
# - openai -> gpt-4o-mini
# ADK_MODEL=gemini-2.0-flash
